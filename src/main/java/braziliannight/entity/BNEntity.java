package braziliannight.entity;

//big TODO
public interface BNEntity
{

  public float onlyRenderTicks = 0; // if we wanted to only render every so often this would be set for that... I
                                    // think?
}
